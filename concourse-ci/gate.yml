---
platform: linux

image_resource:
  type: docker-image
  source: {repository: python, tag: jessie}

inputs:
  - name: csmake-pull-requests

run:
  path: /bin/bash
  args:
  - -exc
  - |
    sudo pip install coverage
    pushd csmake-pull-requests
    ./RUNTESTS.sh
    popd

