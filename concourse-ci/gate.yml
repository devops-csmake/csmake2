---
platform: linux

image_resource:
  type: docker-image
  source: {repository: python, tag: 2-jessie}

inputs:
  - name: csmake-pull-requests

outputs:
  - name: csmake-pull-requests/test-results

run:
  path: /bin/bash
  args:
  - -exc
  - |
    pip install coverage
    pushd csmake-pull-requests
    ./RUNTESTS.sh
    popd

